/// @func _lu(a)
/// @desc Produces the LU factorization of a matrix, decomposing it into a lower triangular and an upper triangular matrix.
/// @param {real[][]} a - Input matrix.
/// @return {real[][][]} Pair of 2D arrays of the form [L, U], where L is lower triangular and U is upper triangular.

function _lu(a)
{
	// Get matrix dimensions
	var m = array_length(a);
	var n = array_length(a[0]);
	
	//###
}
