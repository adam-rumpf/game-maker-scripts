/// @func _matrix_qr(a)
/// @desc Produces the QR factorization of a matrix, decomposing it into a unitery matrix and an upper triangular matrix.
/// @param {real[][]} a - Input matrix.
/// @return {real[][][]} Pair of 2D arrays of the form [Q, R], where Q is unitary and R is upper triangular.

function _matrix_qr(a)
{
	// Get matrix dimensions
	var m = array_length(a);
	var n = array_length(a[0]);
	
	//###
}
